<%= form_with(model: directorio, local: true) do |form| %>
  <% if directorio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(directorio.errors.count, "error") %> prohibited this directorio from being saved:</h2>

      <ul>
      <% directorio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :directorio_nombre %>
  </div>

  <div class="field">
    <%= form.label :apellido %>
    <%= form.text_field :apellido, id: :directorio_apellido %>
  </div>

  <div class="field">
    <%= form.label :telefono %>
    <%= form.number_field :telefono, id: :directorio_telefono %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
